VITE_APP_LIBRARY_URL=https://libraries.excalidraw.com
VITE_APP_LIBRARY_BACKEND=https://us-central1-excalidraw-room-persistence.cloudfunctions.net/libraries

# --- Google OAuth (login + Google Drive) ---
# Client do tipo "Desktop app" criado no Google Cloud Console.
# Estes campos ficam VAZIOS aqui (arquivo versionado); preencha os valores
# reais em app/.env.local (fora do versionamento).
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
